# systemd EnvironmentFile format:
# - NO "export"
# - One KEY=VALUE per line
# - Quote values only if needed
#
# Copy to: /root/Chloe-alpha/systemd/chloe.env

# Core mode
MODE=PAPER

# Enable/disable GPT reflection (must also be enabled in config/engine_config.json)
USE_GPT_REFLECTION=true

# OpenAI credentials (example only)
# OPENAI_API_KEY=sk-...

